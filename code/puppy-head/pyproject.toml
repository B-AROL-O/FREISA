[project]
name = "puppy-head"
version = "0.1.0"
description = "API server to make FREISA change expressions and play sounds"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.2",
]
